[package]
name = "godgame-data"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Compiler output. Generated by `cargo run -p contentc` — never edited by hand."

# This crate must stay dependency-free apart from bitflags. It is linked by the
# sim, the renderer, the benches and the xtask gates, and anything it pulls in is
# pulled into all of them.
[dependencies]
bitflags.workspace = true

[dev-dependencies]
serde_json = "1"
